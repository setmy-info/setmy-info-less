package-lock.json
node_modules
**/dist/**
**/site/**
reports/
build/
.claude/

# LESS formatting is owned by stylelint (`npm run lint` / `lint:fix`), not by
# prettier: stylelint-config-standard's rule-empty-line-before and prettier
# disagree about blank lines between rules, and a repo must have exactly one
# formatter per language. Prettier owns js/cjs/json/md/yml here.
**/*.less
