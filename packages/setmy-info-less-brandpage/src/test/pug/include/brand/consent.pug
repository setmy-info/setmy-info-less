//- brand/consent — #gb consent bar pinned to the bottom of the viewport.
mixin brandConsent()
    div(id="gb")
        p Lorem ipsum dolor sit amet, consectetur adipiscing elit.
        button(id="consentAccept", type="button", class="ga") Accept
