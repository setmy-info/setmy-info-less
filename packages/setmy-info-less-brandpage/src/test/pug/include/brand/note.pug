//- brand/note — .cf footnote, with the .pl inline link button in it.
mixin brandNote()
    p(id="cf", class="cf")
        | Lorem ipsum dolor —
        button(id="privacyLink", type="button", class="pl") sit amet
