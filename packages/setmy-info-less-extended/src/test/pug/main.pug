doctype html
html(lang="en")
    include include/head.pug
    body(id="body" class="defaultPadding")
        app(id="app")
            div(id="application")
                if page == 'abc'
                    include ./partials/abc.pug
                else if page == "experimental-frames"
                    include ./partials/experimental-frames.pug
                else
                    div Error. Page Not found.
