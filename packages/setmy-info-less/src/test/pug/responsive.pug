doctype html
html(lang="en")
    include include/head.pug
    body(id="body")
        div(id="phoneHidden" class="phone-hidden") hidden below 1024px
        div(id="pcHidden" class="pc-hidden") hidden at 1024px and wider
        div(id="always") always visible
