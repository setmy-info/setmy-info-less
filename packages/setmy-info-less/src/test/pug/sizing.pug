doctype html
html(lang="en")
    include include/head.pug
    body(id="body")
        div(id="maxWidth" class="maxWidth")
        div(id="halfWidth" class="halfWidth")
        div(id="quarterWidth" class="quarterWidth")
        div(id="threeQuartersWidth" class="threeQuartersWidth")
        div(id="elementHeight" class="elementHeight")
