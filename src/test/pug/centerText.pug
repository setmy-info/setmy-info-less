doctype html
html(lang="en")
    include head.pug
    body(id="body")
        app(id="app")
            div(id="application")
                div(id="centerText" class="centerText")
                    span(id="spanCenterText") Span text
