doctype html
html(lang="en")
    include include/head
    body(id="body")
        app(id="app")
            div(id="application")
                div(id="centerBox" class="centerBox")
                    if page == 'abc'
                        include ./partials/abc.pug
